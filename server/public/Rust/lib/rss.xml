<rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title><![CDATA[Work]]></title><description><![CDATA[Obsidian digital garden]]></description><link>http://github.com/dylang/node-rss</link><image><url>lib\media\favicon.png</url><title>Work</title><link/></image><generator>Webpage HTML Export plugin for Obsidian</generator><lastBuildDate>Thu, 02 Oct 2025 14:52:20 GMT</lastBuildDate><atom:link href="lib\rss.xml" rel="self" type="application/rss+xml"/><pubDate>Thu, 02 Oct 2025 14:52:14 GMT</pubDate><ttl>60</ttl><dc:creator/><item><title><![CDATA[Traits]]></title><description><![CDATA[ 
 <br>A trait is a contract that requires the types that implement the trait to have certain functionality according to the contract. Imagine them like an interface in C# and Java and abstract classes in C++. They enable polymorphism in Rust.<br><br>Following are some useful built in traits.<br><br>Mandates a format method on the type which can output string representation, used while printing the data using  println!.<br>println!("This is my integer {}", 32);
<br><br>Debug should format the data to a programmer facing string which is useful for debugging.<br>#[derive(Debug)] // easy shortcut to auto add debug trait to types
struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 3, y: 7 };
    println!("{:?}", p);         // Output: Point { x: 3, y: 7 }
    println!("{:#?}", p);        // Pretty-printed
}
<br><br>Mandates that the type can be copied (Full duplicate). This forces the compiler to treat operations such as assignment, passing arg as value as NOT MOVE. Basically something that can be copied using memcpy.<br>let x = 24;
let y = x; // 2 separate copies

#[derive(Copy, Clone)] // For custom types both copy and clone must be derived
struct Point {
	x: i32, // All fields of the type must implement Copy on their own
	y: i32,
}
<br>Copy Behavior for Heap Allocated Types
Heap allocated types such as Vec&lt;T&gt; do not implement the copy trait. Such data types when assigned to another variable a duplicate reference pointing to the same data on the heap is created.
<br><br>Any type can implement this trait unlike copy. This introduces a special method .clone() on the object. This is where you implement complex logic to do clone the whole objects.<br>Copy and Clone are closely related
Copy is essentially derived from clone i.e its a subset.<br>
Copy types must implement Clone because Copy is essentially saying "my clone() operation is so cheap it can happen implicitly.
]]></description><link>advanced\traits.html</link><guid isPermaLink="false">Advanced/Traits.md</guid><pubDate>Tue, 22 Jul 2025 18:28:59 GMT</pubDate></item><item><title><![CDATA[Data Types in Rust]]></title><description><![CDATA[ 
 <br>Data types in Rust are divided into 2 broad categories:<br><br>These data types only hold 1 value. There are 4 scalar types- integers, Booleans, floating point numbers &amp; characters. These scalar types are subdivided into further types.<br><br>
isize &amp; usize are platform dependent for example  for 32 bit architecture.<br>
There is no concept of truthy and falsy values in rust, there is only boolean which is either true or false
<br>int my_variable: u8 = 15;
int my_float = 15.0f32
<br>Numerical Representation
Like other languages, Rust provides syntax to make numeric values more readable, for example- you can have underscores _ between numbers anywhere and rust will ignore those, 100_000. There is also a scientific notation- let c = 6.02e23;    // Avogadro's number-ish (f64)
<br><br>These types store more than 1 value. There are 2 primitive compound types Tuples &amp; Arrays. <br><br>Fixed size collection of homogenous data.<br>let myArray: [f32, 3] = [14.023,15f32,16.2342]; // Array of type f32 and length 3
// All elements can be initialized to the same value.
let ys: [i32; 500] = [0; 500];
<br>Important information about arrays:<br>
<br>Like all other variables, they must be initialized while defining.
<br>There are syntactic sugars for initializing such as [0;5] initializes a 5 element array with all elements having value zero.
<br>Access is done by index operator like other languages my_array[0]
<br>len() method is used to get the length.
<br>All data is by default immutable until the array itself is made mutable.
<br><br>Simple, Fixed-Size &amp; Heterogenous collections. <br>let person = ("Alice", 30, true); // &amp;str, i32, bool
let employee: (&amp;str, i32, f64) = ("Bob", 45, 3.14);
let single = (5u32,); // Tuple with one element requires a comma to distinguish from plain parenthesis
<br>Accessing elements:<br>let t = ("hi", 5, 'c');
println!("{}", t.0); // By index
let (greeting, number, ch) = t; // By destructuring
<br>Unit Tuple 
A tuple with no members (), this is the default return type of functions in case it is not specified implicitly or explicitly.
<br><br>These are non-primitive compound types. There are basically three main strings you need to know about.<br>
<br>String: From the standard library, mutable objects which store collection of characters, created as String::from("Hello") or "Hello".to_string() 
<br>String Literals: Immutable objects stored neither in the <a data-tooltip-position="top" aria-label="Stack and Heap" data-href="Stack and Heap" href="stack-and-heap.html" class="internal-link" target="_self" rel="noopener nofollow">Stack or the Heap</a> but baked in the binary, rust uses interning on them. The above examples to create a String use literals as base. They have internal type of &amp;'static str, sometimes also referred to as slices.
<br>&amp;str: Immutable reference to a UTF-8 string  which is either a literal(the above) or a part of String.
<br>Up Next: <a data-href="Type Casting" href="type-casting.html" class="internal-link" target="_self" rel="noopener nofollow">Type Casting</a><br><br>This is the juice of how you use strings in rust. This section covers it in more detail.<br><br>This function is used to append data to the String. String characters are stored as contiguous memory, so if the heap doesn't have enough capacity in that stack pointer location, a new heap memory is found and the address of the pointer is auto updated.<br>let mut name = String::from("Sameer");
name.push_str(" Trivedi");
<br><br>These are applicable for both Arrays and Strings<br>// Heap allocated string 
let my_string = String::from("hello_world");
let my_slice: &amp;str = &amp;my_string[0..3];
let my_string_from_slices = my_slice.to_string();
// String slices always have same type &amp;str, no matter they were derived from a literal or a String
let mut my_string = String::from("hello_world");
let my_mutable_slice = &amp;mut my_string[0..4]; // Does not work with literals though, only slices of Strings are mutable, those also don't have the String methods, making them very limited
let mut my_array = [1,2,3];
let my_array_slice = my_array[0..1]; // has the full array logic, i.e you can modify an index if you want

// Interesting Observation
 let test = {
        let test = "sims";
        test
    };
    println!("{test}"); // Not out of scope, since &amp;'static str and its slices are baked into the binary there no dangling pointer
<br>Range slices are not based on indices
The range x..y might seem like it will give slice starting from index x till y. This is mostly true, but the x and y are not indexes, but are byte positions, if the character occupies more than one bytes, then rust will panic at runtime, since slicing those characters is not possible. However for Arrays it is based on index 🙈
<br>Deref Coercion
Take a look at the following code:
fn greet(name: &amp;str) {
   println!("Hello, {}!", name);
}
fn main() {
   let owned_name = String::from("Alice");
   let borrowed_name = "Bob";
   greet(&amp;owned_name);    // &amp;String → &amp;str (deref coercion)
   greet(borrowed_name);  // &amp;str → &amp;str (no conversion)
   }

The function greet takes &amp;str, but accepts both &amp;String and &amp;str arguments thanks to Rust's automatic deref coercion. When you pass &amp;owned_name (which is &amp;String), Rust automatically converts it to &amp;str because String implements Deref&lt;Target = str&gt;.
]]></description><link>basics\data-types-in-rust.html</link><guid isPermaLink="false">Basics/Data Types in Rust.md</guid><pubDate>Sat, 02 Aug 2025 18:19:26 GMT</pubDate></item><item><title><![CDATA[Stack and Heap 2025-07-22 22.35.17]]></title><description><![CDATA[ 
 ProcessMemoryStaticHeapCodeThreadRegistersStackThreadRegistersStackThreadRegistersStack]]></description><link>excalidraw\stack-and-heap-2025-07-22-22.35.17.excalidraw.html</link><guid isPermaLink="false">Excalidraw/Stack and Heap 2025-07-22 22.35.17.excalidraw.md</guid><pubDate>Tue, 22 Jul 2025 17:10:42 GMT</pubDate></item><item><title><![CDATA[Constants in Rust]]></title><description><![CDATA[ 
 <br>For a constant in rust the following hold true: <br>
<br>Its value never changes.
<br>It must be declared and initialized at the same time means the value must be known at compile time.
<br>Cannot be shadowed
<br>Can live in a global scope unlike Variables.
<br><br>const MY_CONSTANT = 24.5;
]]></description><link>constants-in-rust.html</link><guid isPermaLink="false">Constants in Rust.md</guid><pubDate>Sun, 20 Jul 2025 11:25:29 GMT</pubDate></item><item><title><![CDATA[Control Flow]]></title><description><![CDATA[ 
 <br>The if statement is weird, look at the syntax<br>if expression { // you dont need parenthesis here
	
} // but you need the curly braces
<br>And yes you have else and else if like any sane language<br>if expression {
	
}
else if expression2 { // Pardon my .NET psychopath habits
	
}
else {

}
<br><br>Yes due to implicit returns and if-else ladder being technically a block, you can assign values out of it.<br>let age = 17;
    let voting_allowed: bool = if age &gt;= 18 { true } else { false }; // This whole line starting from let is an "expression" the if is a "statement" which returns a value but this whole line is an expression.
    println!("{}", voting_allowed);
<br>Read More: <a data-tooltip-position="top" aria-label="Functions > ^statement-vs-expressions" data-href="Functions#^statement-vs-expressions" href="functions.html#^statement-vs-expressions" class="internal-link" target="_self" rel="noopener nofollow">Statements vs Expressions</a><br><br>They went with "match" like our good old snake_friend 🐍. Below is the syntax-<br>let evalution: bool = true;

match evaluation { // no break needed like other languages
	// Patterns/arms
	true =&gt; {
 		// do something
	}
	false =&gt; {
		// do something else
	}
	_ =&gt; {
		// default, I know its a sh^t example
	}
}
<br>
A good safety feature of rust is that it checks whether all possible paths have been covered before the code can compile in a match statement.
<br>And assignment also works like above.<br>Fallthrough and multi-match
By design rust match arms don't have a break statement, so it does not support fallthrough statements like if and if-else. To do a multi-match you follow the below syntax. Even the curly braces are optional, you can just write a single line after =&gt;
match x {
1 | 2 =&gt; { /* shared logic for 1 and 2 */ },
_ =&gt; { /* rest */ }
}

<br>
Every match arm must return a value because the RHS of the =&gt; is a "statement" and they must be of same type, if it doesn't return anything it returns a <a data-tooltip-position="top" aria-label="Functions > ^cbdcdc" data-href="Functions#^cbdcdc" href="functions.html#^cbdcdc" class="internal-link" target="_self" rel="noopener nofollow">Unit Tuple</a>
<br><br>In this episode or Rusty Voodoo we are looking at matched if-else.<br>match x {
    n if n &gt; 0 =&gt; {
        if n % 2 == 0 {
            "positive even"
        } else {
            "positive odd"
        }
    }
    n if n &lt; 0 =&gt; {
        if n % 2 == 0 {
            "negative even"
        } else {
            "negative odd"
        }
    }
    0 =&gt; "zero",
    _ =&gt; "unknown",
}
<br>Read More: <a data-tooltip-position="top" aria-label="Macros > `unreachable` macro" data-href="Macros#`unreachable` macro" href="macros.html#`unreachable`_macro" class="internal-link" target="_self" rel="noopener nofollow">Unreachable macro</a><br>Up Next: <a data-href="Loops" href="loops.html" class="internal-link" target="_self" rel="noopener nofollow">Loops</a>]]></description><link>control-flow.html</link><guid isPermaLink="false">Control Flow.md</guid><pubDate>Thu, 02 Oct 2025 14:22:43 GMT</pubDate></item><item><title><![CDATA[Enums]]></title><description><![CDATA[ 
 <br>A special type that represents a group of constants.<br>
enum Color{
    Red,
    Blue,
    Yellow,
    Green
}

let treeColor: Color = Color::Green;
<br><br>Rust enums are powerful; each variant can store different types and amounts of data.<br>enum Message {
    Quit,              // Unit variant, no data
    Move { x: i32, y: i32 },  // Struct variant with named fields
    Write(String),     // Tuple variant with one anonymous field
    ChangeColor(i32, i32, i32),  // Tuple variant with multiple fields
}
<br>
<br>Unit variants like Quit store no data.
<br>Tuple variants like Write or ChangeColor hold anonymous data.
<br>Struct variants like Move have named fields for structured data.
<br>About memory allocation
The amount of memory rust allocates for a struct is &gt;= largest_size_among_all_fields, when creating the struct it atleast allocates this much. They live on heap as expected.
<br><br>The match expression becomes very powerful when used with enums, since rust checks that all possible values of that enum must be covered in the match statement, below is an example: <br>enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let color = Color::Red;
    match color {
        Color::Red =&gt; println!("Red"),
        Color::Green =&gt; println!("Green"),
        Color::Blue =&gt; println!("Blue"),
    }
}
]]></description><link>enums.html</link><guid isPermaLink="false">Enums.md</guid><pubDate>Thu, 02 Oct 2025 14:43:05 GMT</pubDate></item><item><title><![CDATA[Functions]]></title><description><![CDATA[ 
 <br>I am not going to define functions, that would be absurd. Below is the syntax however if I ever get dementia. Rust is a functional programming language so you have pretty much all function functionalities with added extras.<br>Parameters vs Arguments
Functions&nbsp;may declare a set of input variables as&nbsp;parameters, through which the caller passes&nbsp;arguments&nbsp;into the&nbsp;function
<br>fn my_function(){
	// Body here
}
<br>Well there are a few differences between the functions you see in other languages-<br><br>You don't have to add an explicit return statement for functions. <br>fn add(x: i32, y: i32) -&gt; i32 { // The return type is a must when your function returns anything other than a unit tuple 
    x + y  // implicit return (no semicolon)
}

// This behaviour is not limited to functions, any block's last statement is an implicit return
let x = {
	let k = 2.14;
	k*k
}
<br>Notice the semi-colon (Statements vs Expressions)
Statements in rust end with a semi-colon and do not return a value, Expressions don't end with a semi-colon and do return a value, in the implicit return the last line has to be an expression for it to work. Furthermore if you specified a return type in the function definition and then add a statement at the end of the function, it will trigger a compile time error.<br>

<br><br>Parameters can take ownership, borrow immutably, or borrow mutably:<br>fn take_ownership(s: String) { /* s is moved here */ }
fn borrow_immutably(s: &amp;String) { /* s is borrowed */ }
fn borrow_mutably(s: &amp;mut String) { /* s is mutably borrowed */ }
<br>
The default return type for a function without any explicit or implicit returns i.e. void is a Unit Tuple
]]></description><link>functions.html</link><guid isPermaLink="false">Functions.md</guid><pubDate>Sun, 20 Jul 2025 11:49:53 GMT</pubDate></item><item><title><![CDATA[Loops]]></title><description><![CDATA[ 
 <br><br>⚠️ Another because I said so syntax ahead-<br>loop {
	println!("Infinite loop");
}
<br>In the loop keyword you have to manually break out of the loop using break statement when you are done. There is also a continue statement.<br>fn main() {
    for i in 1..10 {
        if i == 3 {
            continue;
        }
        if i == 7 {
            break;
        }
        println!("{}", i);
    }
}
<br>
The loop loop can return a value as well, this is done using the break statement break value_to_return; This only works in the loop loop
<br><br>Standard while loop nothing special.<br>let mut i = 0;
while i &lt; 15 {
	i += 1;
	println!("{i}");
}
<br>
<br>Break and continue work the same way but unlike loop loop, it cannot return via break.
<br><br>The good ol' for-loop<br>fn main() {
    // 1. Basic for loop over a range (exclusive end)
    println!("Basic range (1..6):");
    for i in 1..6 {
        println!("  {}", i); // Prints 1 to 5
    }

    // 2. Inclusive range (includes the end)
    println!("\nInclusive range (1..=6):");
    for i in 1..=6 {
        println!("  {}", i); // Prints 1 to 6
    }

    // 3. Iterating over an array
    let fruits = ["Apple", "Orange", "Banana"];
    println!("\nArray iteration:");
    for fruit in fruits {
        println!("  {}", fruit); // Prints each fruit
    }

    // 4. Index-based iteration over an array
    let colors = ["red", "green", "blue"];
    println!("\nIndex-based iteration:");
    for i in 0..colors.len() {
        println!("  The color name is {}", colors[i]); // Uses index to access element
    }

    // 5. Using enumerate to get (index, value) pairs
    let items = ["x", "y", "z"];
    println!("\nEnumerate for index and value:");
    for (index, value) in items.iter().enumerate() {
        println!("  {}: {}", index, value); // Prints index and value
    }

    // 6. Reverse iteration (descending order)
    println!("\nRange reversed (1..6).rev():");
    for i in (1..6).rev() {
        println!("  {}", i); // Prints 5 to 1
    }

    // 7. Stepping by intervals (e.g., every 2nd number)
    println!("\nRange with step_by(2) (0..10).step_by(2):");
    for i in (0..10).step_by(2) {
        println!("  {}", i); // Prints 0, 2, 4, 6, 8
    }
}
<br>Read More: <a data-href="Recursions" href="Recursions" class="internal-link" target="_self" rel="noopener nofollow">Recursions</a>]]></description><link>loops.html</link><guid isPermaLink="false">Loops.md</guid><pubDate>Sun, 20 Jul 2025 12:37:26 GMT</pubDate></item><item><title><![CDATA[Macros]]></title><description><![CDATA[ 
 <br>In Rust, macros are a powerful metaprogramming feature that let you write code that writes code. They allow you to define patterns and generate code at compile time, which can reduce boilerplate, improve readability, or enable complex functionality.<br>Metaprogramming
Concept in computer programming where, a program can treat other programs as input data
<br>There are 3 kinds of macros in rust:<br><br>The most straight forward macros<br>macro_rules! say_hello { // Macros are created using macro_rules! macro
    () =&gt; {
        println!("Hello!");
    };
}

fn main() {
    say_hello!(); // Prints "Hello!"
}
<br><br>These are more powerful and flexible, and work on Rust code as input (AST manipulation). They're like compiler plugins which generate code from macros.<br>use proc_macro;

#[some_attribute]
pub fn some_name(input: TokenStream) -&gt; TokenStream {
}
<br><br><br>It prints the value of an expression along with the file name and line number where it’s used.<br>fn main() {
    let a = 2;
    let b = dbg!(a * 3) + 1;

    dbg!(b);
}

/* Output
[src/main.rs:4] a * 3 = 6
[src/main.rs:6] b = 7
*/
<br><br>Can be passed written in the arms of a conditional where you are sure that the control won't reach to silence the return requirements by the compiler<br>fn describe_day(day: u8) -&gt; &amp;'static str {
    match day {
        1..=5 =&gt; "Weekday",
        6 | 7 =&gt; "Weekend",
        _ =&gt; unreachable!("'day' should be between 1 and 7"),
    }
}

fn main() {
    println!("{}", describe_day(2)); // Weekday
    println!("{}", describe_day(6)); // Weekend
    // println!("{}", describe_day(8)); // Panics with message
}
]]></description><link>macros.html</link><guid isPermaLink="false">Macros.md</guid><pubDate>Sun, 20 Jul 2025 12:05:50 GMT</pubDate></item><item><title><![CDATA[Ownership]]></title><description><![CDATA[ 
 <br>Ownership is a set of compile-time rules that the compiler checks for to ensure the program will be free of memory errors.<br>Owner is something that is responsible for cleaning up a piece of memory when it is no longer in use. Every value in a rust program has a 1 owner. The owner can change over the course of a program.<br>Move this refers to transfer of ownership from one owner to another in rust.<br>let name = String::from("Sameer"); // lives in heap, name stores the **pointer**
let next = name; // Ownership transferred, now next is the ONLY pointer pointin to the String
println!("{name}"); // name is useless now, this won't compile.
<br>Rust's Borrowing Rules<br>Rust allows multiple ways to access data, but with strict safety rules:<br>Immutable References (&amp;T)<br>
<br>You can create multiple immutable references to the same data
<br>These references only allow reading the data, not modifying it
<br>Multiple immutable references can coexist safely since no one can change the data
<br>Mutable References (&amp;mut T)<br>
<br>Only one active mutable reference can exist at a time
<br>This reference allows both reading and modifying the data
<br>No other references (mutable or immutable) can exist while a mutable reference is active
<br>The Key Rule: Exclusive Access At any given time, you can have either:<br>
<br>Multiple immutable references, OR
<br>Exactly one active mutable reference
<br>A reference is considered "active" from when it's created until its last use, not just until the end of its scope. Below is an example:<br>let mut data = vec![1, 2, 3];
let immut_ref = &amp;data;        // immutable reference created
println!("{:?}", immut_ref);  // last use of immutable reference
let mut_ref = &amp;mut data;      // mutable reference created (OK!)
mut_ref.push(4);              // use mutable reference
<br>
Immutable references implement the copy trait.
<br>The drop function
drop(&amp;mut self) function is a special function which is called by Rust at the end of a scope which cleans up the data in the heap or signals the rust runtime to do so. This only works on heap allocated memory. You can also define the drop function which is a part of the Drop trait, for custom types. Also it can be manually called whenever you want for the types that implement it, this makes them unusable.
let mut person = String::from("Test");
drop(person);
println!("{person}");

<br><br><br><br>fn consume_string(s: String) { // This can also be mut s: String even if the previous owner is not mutable
    println!("{}", s);
    // s is dropped here
}

let my_string = String::from("hello");
consume_string(my_string); // my_string is moved, no longer accessible
<br><br>fn read_string(s: &amp;String) {
    println!("{}", s);
}

let my_string = String::from("hello");
read_string(&amp;my_string); // my_string is still accessible after the call
<br><br>fn modify_string(s: &amp;mut String) {
    s.push_str(" world");
}

let mut my_string = String::from("hello");
modify_string(&amp;mut my_string);
println!("{}", my_string); // prints "hello world"
<br><br>fn process_str(s: &amp;str) { // Immutable reference
    println!("{}", s);
}

// Works with both String and &amp;str
let owned = String::from("hello");
let borrowed = "world";
process_str(&amp;owned);
process_str(borrowed);
<br>When functions return a variable, there is again an ownership transfer, following the same set of rules. The function is no longer responsible for <a data-tooltip-position="top" aria-label="^e064ef" data-href="#^e064ef" href="about:blank#^e064ef" class="internal-link" target="_self" rel="noopener nofollow">drop</a>. <br><br>Compound types such as <a data-tooltip-position="top" aria-label="Data Types in Rust > Compound Types" data-href="Data Types in Rust#Compound Types" href="basics\data-types-in-rust.html#Compound_Types" class="internal-link" target="_self" rel="noopener nofollow">Arrays &amp; Tuples</a>, own each element inside them. So when their elements are accessed, a full copy needs to be created or access must be done via reference.<br>let my_array = [1,2,3];
let el = my_array[0]; // Full copy created, only possible when copy trait implemented by type
let my_array_2 = [String::from("Test")]
let el_2 = my_array_2[0] // Throws error, either .clone() must be used or el_2 must hold a reference.
]]></description><link>ownership.html</link><guid isPermaLink="false">Ownership.md</guid><pubDate>Mon, 28 Jul 2025 18:12:49 GMT</pubDate></item><item><title><![CDATA[Reference & Borrowing]]></title><description><![CDATA[ 
 <br>
Borrowing means taking something without transferring ownership, a borrower must return the reference.
<br>Borrowing involves creating a reference to a value. Generally Heap Data's reference are used, but stack data can also be referenced. <br>let my_val: i32 = 2;
let my_ref: &amp;i32 = &amp;my_val; // Type address leading to i32
<br>References must never outlive their referent.<br>println!("{*my_ref}");
<br>Rust auto dereferences display trait implementing types]]></description><link>reference-&amp;-borrowing.html</link><guid isPermaLink="false">Reference &amp; Borrowing.md</guid><pubDate>Sat, 26 Jul 2025 18:09:20 GMT</pubDate></item><item><title><![CDATA[Stack and Heap]]></title><description><![CDATA[ 
 <br>Yes these are the same ones you studied in Operating Systems class. Stack and Heap are 2 memory regions where data is stored during program execution. <br><br>Good old stack, below is the diagram which tells you "where" this stack is<br>ProcessMemoryStaticHeapCodeThreadRegistersStackThreadRegistersStackThreadRegistersStack<br>Like any other stack, this is also a LIFO structure. Very fast for allocation and deallocation.<br>In Rust stack is used for storing:<br>
<br>Scalar types (integers, booleans, characters)
<br>Fixed size arrays
<br>Structs and enums where all fields have known sizes
<br>Function parameters etc.
<br><br>The location of heap is also mentioned in the above drawing. This is used for data which is unknown or has variable type at compile time. Allocation is slower than the stack and pointers are required for data access.<br>Rust stores the following in heap:<br>
<br>Data types with dynamic or unknown size, such as Vec&lt;T&gt;
<br>Large data, only pointer to the data resides on the stack.
<br>If data needs to outlive the function or scope where it was created, heap allocation is often necessary.
<br>Box pointer 📦
All values in Rust are stack allocated by default. Values can be&nbsp;boxed&nbsp;(allocated on the heap) by creating a&nbsp;Box&lt;T&gt;. A box is a smart pointer to a heap allocated value of type&nbsp;T. When a box goes out of scope, its destructor is called, the inner object is destroyed, and the memory on the heap is freed.
<br>Memory safety without a Garbage Collector?
Rust claims to be memory safe, but it does not have a garbage collector. Well there are a lot of tricks in the crabs claws, one of them is automatically calling the destructor of an allocated value once it goes out of scope.
<br>See also: <a data-tooltip-position="top" aria-label="Traits > ^1a31c0" data-href="Traits#^1a31c0" href="advanced\traits.html#^1a31c0" class="internal-link" target="_self" rel="noopener nofollow">Copy trait</a>]]></description><link>stack-and-heap.html</link><guid isPermaLink="false">Stack and Heap.md</guid><pubDate>Tue, 22 Jul 2025 17:56:25 GMT</pubDate></item><item><title><![CDATA[Structs]]></title><description><![CDATA[ 
 <br>A struct or structure is a container for related pieces of data.<br>There are 3 types of structs in rust:<br>
<br>Named Type Structs: Fields have names, this is the most common type of struct.
<br>Tuple structs: These are like tuples but have a name. Which can be used to create more instances of the struct type.
<br>Unit structs: These structs don't have any fields. These are used when you want a type to implement traits but don't want to store any data.
<br>/* Named Type Structs */
struct Person {
	name: String,
	age: u8 
}

let heroine = Person {
	name: String::from("Daenerys Targaryen"),
	age: 197
}; // Notice the delimiter

println!("{} was the rightful heir to the Iron Throne", heroine.name);

/* Tuple structs */
struct Rgb(u8, u8, u8);

let black = Rgb(255,255,255);

println!("R value of black is: {Rgb.0}");

/* Unit structs */
struct Marker;

let m1 = Marker; 
<br>Mutability of Fields
All fields are either mutable or immutable depending on whether the variable that owns the struct is declared as&nbsp;mut 
<br>Field Init Shorthand and Spreading
Like TS and JS Object property shorthand, rust has a handy syntactical sugar for doing below
struct Player {
    class: String,
    name: String, 
    level: u8
}

fn create_player(class: String, name: String, level: u8) -&gt; Player {
    Player {
        class,
        level,
        name 
    }   
}

Apart from this it also has spreading, which in Rust is called "struct update syntax":
fn level_up_player(player: Player) -&gt; Player {
    Player {
        level: player.level + 1,
        ..player // Must be written last in the syntax field and rust will not replace anything that was declared before the spread
    }
}

// Example usage:
let player1 = Player {
    class: String::from("Warrior"),
    name: String::from("Alice"),
    level: 5
};

let player2 = level_up_player(player1);
// player2 will have class: "Warrior", name: "Alice", level: 6

<br><br>We can define functions on structs, a function can either belong to instance of struct or struct itself (called associated function)<br>const PI: f64 = 3.14;
struct Circle {
	radius: f64
}

impl Circle {
	fn calc_area(&amp;self: Self) -&gt; f64 { // You can also pass self and mut self which will result in an ownership transfer
		PI * self.radius * self.radius
	}
	fn increase_radius(&amp;mut self: &amp;mut Circle, increase_by: f64) {
		self.radius += increase_by;
	}
	// Associated function (does not take self)
	fn get_sides() -&gt; u8 {
		0	
	}
}
<br>
A struct can define multiple impl blocks, during compilation it will combine all the blocks to create the final implementation.
<br><br>Every method returns the instance itself. Like in C#<br>builder.AddOptions().AddHttpClient().AddControllers();
<br>Its also possible in rust: <br>#[derive(Clone)]
struct Builder {
    options: Vec&lt;String&gt;,
}

impl Builder {
    fn new() -&gt; Self {
        Self {
            options: Vec::new(),
        }
    }

    fn add_option(mut self, opt: String) -&gt; Self { // we can also return a mutable reference
        self.options.push(opt);
        self
    }

    fn build(self) -&gt; Vec&lt;String&gt; {
        self.options
    }

fn main() {
    let builder = Builder::new()
        .add_option("option1".to_string())
        .add_option("option2".to_string());

    let result = builder.build();
    println!("{:?}", result);
}
]]></description><link>structs.html</link><guid isPermaLink="false">Structs.md</guid><pubDate>Thu, 02 Oct 2025 14:07:07 GMT</pubDate></item><item><title><![CDATA[Crates in Rust]]></title><description><![CDATA[ 
 <br>A crate is a unit of compilation in rust, simply put it can be called as a package of rust code.<br>There are two types of crates in rust:<br>
<br>Binary Crates: Crates that contain executable code. They contain a single main.rs with a main() method.
<br>Library Crates: Crates that don't contain executable code and are used to ship reusable code by linking them to other crates. Does not have a main method.
<br>Dual Crates?
A crate can ship as both a binary and a library crate. In such cases the add command links the library portion of the crate, whereas the install command installs the executable.
<br><br>Like nuget &amp; npm there is a central repository for rust crates. Below are some useful commands -<br>cargo add crate_name # adds the dependency of the crate_name in current package, ONLY works with library crates
cargo install crate_name # installs the executable globally on your machine 
<br>crates.io: A Cautionary Tale
Cargo install, installs rust crates globally on your machine, although the rust crates are audited but there has been multiple incidents of <a data-tooltip-position="top" aria-label="https://www.reddit.com/r/rust/comments/1605fqj/rust_malware_staged_on_cratesio/" rel="noopener nofollow" class="external-link" href="https://www.reddit.com/r/rust/comments/1605fqj/rust_malware_staged_on_cratesio/" target="_blank">fake crates</a>. Better be safe with this command. Also there was some <a data-tooltip-position="top" aria-label="https://news.ycombinator.com/item?id=42972525" rel="noopener nofollow" class="external-link" href="https://news.ycombinator.com/item?id=42972525" target="_blank">Rust Drama</a> where some popular package maintainers self deprecated their packages over a fight.
<br>See Also: <a data-href="Modules in Rust" href="basics\modules-in-rust.html" class="internal-link" target="_self" rel="noopener nofollow">Modules in Rust</a>]]></description><link>basics\crates-in-rust.html</link><guid isPermaLink="false">Basics/Crates in Rust.md</guid><pubDate>Sun, 06 Jul 2025 20:07:25 GMT</pubDate></item><item><title><![CDATA[Creating a Rust Project]]></title><description><![CDATA[ 
 <br>The standard way to do this is using cargo.<br>cargo new project_name
<br>This command:<br>
<br>Creates a folder named hello_world
<br>Auto initializes a git repository inside that folder
<br>Creates an src subfolder with a main.rs file.
<br>Creates a Cargo.toml file for library and version info.
<br>The content of main.rs looks like:<br>fn main() {
    println!("Hello, world!");
}
<br>This command actually creates a so-called binary crate. See <a data-href="Crates in Rust" href="basics\crates-in-rust.html" class="internal-link" target="_self" rel="noopener nofollow">Crates in Rust</a><br><br>There are a couple of ways of doing this:<br><br>rustc main.rs
<br>This produces an executable that then can be run normally.<br><br>The standard way to go for projects<br>cargo build # builds and produces a binary which needs to be separately executed
cargo run # build and run in single step
cargo check # checks the program whether it can be compiled, usually faster but IDEs do this job most of the time
cargo clean # removes all old compiled objects and binaries
<br>Build Flags
By default the cargo commands do a debug build of the program, which is bulky non optimized version useful for debugging and sifting through instructions. The build and run command accept a build flag which can be used to change the target executable cargo build --release. There are several other build flags in rust, can be found <a data-tooltip-position="top" aria-label="https://doc.rust-lang.org/cargo/commands/cargo-build.html" rel="noopener nofollow" class="external-link" href="https://doc.rust-lang.org/cargo/commands/cargo-build.html" target="_blank">here</a>
<br>Built in Autoformatting
rustfmt file_name can be used to auto format a rust file, cargo fmt does the same thing for the whole project.
]]></description><link>basics\creating-a-rust-project.html</link><guid isPermaLink="false">Basics/Creating a Rust Project.md</guid><pubDate>Mon, 07 Jul 2025 07:47:03 GMT</pubDate></item><item><title><![CDATA[IO]]></title><description><![CDATA[ 
 <br><br>The println! macro supports interpolation using curly braces,<br>var my_age = 24;
var my_name = "Sameer";
println!("My age is {}", my_age); // outputs My age is 24
println!("My age is {my_age}"); // works the same way how cool is that
println!("My age is {0} &amp; My Name is {1}, My Name was always {1}, I turned {my_age} last year", my_age, myname); // positional arguments start from 0 and can be reused in the target string
<br><br>println! macro provides an option of format specifiers which control how certain data will be printed. Works with all interpolations, just follow with the specifier.<br>Examples<br>let name = "Alice";
let age = 30;
println!("{} is {} years old.", name, age);

println!("{:&gt;5}", 42);       // right-align
println!("{:&lt;5}", 42);       // left-align
println!("{:^7}", 42);       // center-align

println!("{:08.2}", 3.1415); // total 8 chars incl decimal = pad + float precision → 00003.14

println!("{name} likes {lang}", name = "Bob", lang = "Rust");

let arr = [1, 2, 3];
println!("{:?}", arr);       // Debug print

<br>There are a lot of these specifiers <br>]]></description><link>basics\io.html</link><guid isPermaLink="false">Basics/IO.md</guid><pubDate>Wed, 09 Jul 2025 17:58:22 GMT</pubDate></item><item><title><![CDATA[Modules in Rust]]></title><description><![CDATA[ 
 <br>Modules are subdivisions inside crates which are used to organize code. Below is an example folder structure<br>my_dual_crate
 ├── src
 │   ├── main.rs     ← crate root (binary)
 │   ├── lib.rs     ← crate root (library)
 │   └── math.rs    ← module
]]></description><link>basics\modules-in-rust.html</link><guid isPermaLink="false">Basics/Modules in Rust.md</guid><pubDate>Sun, 06 Jul 2025 20:09:21 GMT</pubDate></item><item><title><![CDATA[Compiler Directives]]></title><description><![CDATA[ 
 ]]></description><link>compiler-directives.html</link><guid isPermaLink="false">Compiler Directives.md</guid><pubDate>Mon, 07 Jul 2025 09:27:15 GMT</pubDate></item><item><title><![CDATA[Documentation]]></title><description><![CDATA[ 
 <br><br>// line comments
/* block
	commment
*/
<br><br><br>These describe the item they’re inside, typically at the top of a crate or module.<br>
Example: Documenting the entire crate:<br>//! This crate provides utilities for working with geometrical shapes.
<br>Or at the start of a module:<br>mod math {
//! Math utilities 
}
<br><br>These document the item that follows, such as functions, structs, enums. Example:<br>/// Adds two numbers together.
///
/// # Examples
///
/// ```
/// let sum = add(2, 3);
/// assert_eq!(sum, 5);
/// ```
fn add(a: i32, b: i32) -&gt; i32 {
    a + b
}
]]></description><link>documentation.html</link><guid isPermaLink="false">Documentation.md</guid><pubDate>Mon, 07 Jul 2025 07:45:15 GMT</pubDate></item><item><title><![CDATA[Generics in Rust]]></title><description><![CDATA[ 
 <br>Generics are abstract stand-ins for concrete types. You define them using angle brackets &lt;&gt;<br>struct Point&lt;T&gt; {
    x: T,
    y: T,
}

let integer = Point { x: 1, y: 2 };
let float = Point { x: 1.1, y: 2.2 };
]]></description><link>generics-in-rust.html</link><guid isPermaLink="false">Generics in Rust.md</guid><pubDate>Fri, 11 Jul 2025 19:06:06 GMT</pubDate></item><item><title><![CDATA[Rust]]></title><description><![CDATA[ 
 <br><br>Rust is a systems programming language, meaning it is generally deployed in places where system resource (such as CPU, Memory, Bandwidth) usage is critical. <br>Rust is advertised to be safe, fast &amp; efficient programs.<br>Up Next: <a data-href="Creating a Rust Project" href="basics\creating-a-rust-project.html" class="internal-link" target="_self" rel="noopener nofollow">Creating a Rust Project</a>]]></description><link>index.html</link><guid isPermaLink="false">index.md</guid><pubDate>Sun, 06 Jul 2025 20:05:19 GMT</pubDate></item><item><title><![CDATA[Methods]]></title><description><![CDATA[ 
 <br>
Methods are functions that are defined on types
<br>Below is an example on an integer and an &amp;str.<br>let my_var: i32 = -15;
println!("The absolute value is: {}", my_var.abs()); // built-in method that returns the modulus of the integer.
let s = "hello";
println!("{}", s.len());  // method on &amp;str
<br>Methods can't be called on inferred type variables.
You must explicitly mention the type of the variable using : to be able to call methods, otherwise rust throws an ambiguous type error and program fails to compile.
<br>We can define custom methods on types, but only the types we create. It does not work on built in types.<br>struct Circle {
    radius: f64,
}

impl Circle {
    // Method: takes self by reference
    fn area(&amp;self) -&gt; f64 {
        3.1415 * self.radius * self.radius // don't use magic numbers kids 🔫
    }

    // Associated function: doesn't take self like Static
    fn new(radius: f64) -&gt; Self {
        Circle { radius }
    }
}

fn main() {
    let c = Circle::new(5.0);    // associated function (like a constructor)
    println!("Area: {}", c.area());  // method call on instance
}
]]></description><link>methods.html</link><guid isPermaLink="false">Methods.md</guid><pubDate>Wed, 09 Jul 2025 17:51:35 GMT</pubDate></item><item><title><![CDATA[Naming Conventions]]></title><description><![CDATA[ 
 <br>Below table neatly summarizes the naming conventions used in Rust.<br>]]></description><link>naming-conventions.html</link><guid isPermaLink="false">Naming Conventions.md</guid><pubDate>Mon, 07 Jul 2025 07:51:04 GMT</pubDate></item><item><title><![CDATA[Operators]]></title><description><![CDATA[ 
 <br>Rust supports all common operators, and they are not batshit crazy to floor towards negative infinity for negative divisions. And there is no crazy things like == &amp; ===.  There is no ++ and -- cause who needs that but all compoud assignment operators are there +=, |= and all that.]]></description><link>operators.html</link><guid isPermaLink="false">Operators.md</guid><pubDate>Wed, 09 Jul 2025 18:12:30 GMT</pubDate></item><item><title><![CDATA[Ranges]]></title><description><![CDATA[ 
 <br>A range is a sequence of consecutive values.<br>similar to python range<br>let my_range = 1..5;
<br>
<br>Below table summarizes range types in rust-
<br><br>
<br>Ranges implement Iterator, so anything which implements that can have be the type in Range&lt;T&gt;
<br>let my_char_range = 'b'..'q';
<br>
<br>Ranges can be used for Array Slicing
<br>let arr = [0, 1, 2, 3];
assert_eq!(arr[1..3], [1,2]);
<br><br>Below is an example with a for loop<br>for i in 1..=6 {
	println!(i);
}
for i in (1..=6).rev() {
 // Methods are present on Range type
 // Others include .step_by(val)
}
]]></description><link>ranges.html</link><guid isPermaLink="false">Ranges.md</guid><pubDate>Fri, 11 Jul 2025 19:03:20 GMT</pubDate></item><item><title><![CDATA[Type Aliases]]></title><description><![CDATA[ 
 <br>You can nickname types in rust to increase readability in code, For example:<br>type Meters = i32; // Convention is PascalCase

fn main(){
	var track_length: Meters = 400;
}
]]></description><link>type-aliases.html</link><guid isPermaLink="false">Type Aliases.md</guid><pubDate>Mon, 07 Jul 2025 09:26:08 GMT</pubDate></item><item><title><![CDATA[Type Casting]]></title><description><![CDATA[ 
 <br>Rust supports type casting with the as keyword. As long as its a plausible conversion.]]></description><link>type-casting.html</link><guid isPermaLink="false">Type Casting.md</guid><pubDate>Wed, 09 Jul 2025 17:59:57 GMT</pubDate></item><item><title><![CDATA[Variables and Mutability]]></title><description><![CDATA[ 
 <br><br>Below is how you declare a variable in rust<br>let my_var = 50; // compiler auto deduces type 'i32' in this case
<br>Rust variables are immutable by default, meaning once assigned they cannot be re-assigned.<br>my_var = 90; // will throw and error during compilation
let mut myMutableVar = 50; // This defines a mutable variable which CAN be re-assigned
<br><br>Rust supports shadowing (surprise, surprise!), meaning a variable can be redeclared in the same scope and the earlier declaration is invalidated (only occurs when done in the same scope).<br>let my_var = 50; // i32 as of now
let my_var = "text"; // &amp;str now
// mut or non mut does not matter
<br>The Crab Hates Global Variables
So much so that variable declarations can only be done inside a local scope.
<br>Related: <a data-href="Naming Conventions" href="naming-conventions.html" class="internal-link" target="_self" rel="noopener nofollow">Naming Conventions</a>, <a data-href="Data Types in Rust" href="basics\data-types-in-rust.html" class="internal-link" target="_self" rel="noopener nofollow">Data Types in Rust</a>]]></description><link>variables-and-mutability.html</link><guid isPermaLink="false">Variables and Mutability.md</guid><pubDate>Wed, 09 Jul 2025 16:58:29 GMT</pubDate></item></channel></rss>