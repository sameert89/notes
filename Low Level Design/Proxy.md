> A **proxy** is a middleman, which controls access to the real object.

### Why do I need a Proxy?
 A proxy is useful when you want to control, filter, or secure access to the real object without exposing it directly. It acts as an intermediary