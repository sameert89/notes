You don't need a *silver bullet* concept to achieve abstraction. A simple class with private methods is enough.

There is interfaces and abstract classes, in this case the abstraction is being able to swap implementations without having to modify the upstream.