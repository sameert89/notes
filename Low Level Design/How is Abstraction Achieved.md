You do not need a *silver bullet* concept to achieve abstraction. A simple class with private methods is enough.

Interfaces and abstract classes provide another form of abstraction: implementations can be swapped without modifying upstream code.
