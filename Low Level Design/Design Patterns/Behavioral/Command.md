> 	Instead of doing `object.doSomething()` command converts it into `somethingCommand(object)` this converts the action to a **first class object** means now you can *store,queue,undo, retry or schedule* it.

