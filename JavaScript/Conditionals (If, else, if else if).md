This is standard, like C.

# Switch-Case
They are the same as cpp.
```js
var myDay = prompt("enter the day of the week")
switch(myDay){
	case 'monday': {
		// something
		break; // prevents fall through
	}
	case default: {
		// curly braces are optional too, you can have one line follow
	}
}
```