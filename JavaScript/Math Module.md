```js
'use strict'
let x = Math.random(); // Random Number
console.log(Math.trunc(x)); // trunc, floor, ceil, round all are available
const myArray = [1, 2, 3, 4, 5];
console.log(Math.max(myArray)); // There is min as well
console.log((2.333333).toFixed(3));****
```
