```cpp
// the removal can be done anywhere as long as:
        // slice doesnt cut just one row or column, in this case only corners can be removed
        // with additional warning that if there is a single row/column then that cannot be removed
```