#needcode150 #sliding_window 

Problem Link: https://leetcode.com/problems/permutation-in-string/description/

The bruteforce solution is to generate all permutations of the given string and check if each permutation is a substring or not. This results in an $O(n!)$ time complexity and similar space complexity due to recursion depth.